# pratt-info-638 Spring 2026
The class project we're created during the spring semester

## Running the code
Make sure all the dependencies are set up `npm install` <br>
Next you can run the app with `node bookedin.js`